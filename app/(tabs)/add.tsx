import React from "react";
import { View } from "react-native";

export default function AddTabPlaceholder() {
  // This screen is intentionally left blank for the custom middle button
  return <View />;
}
